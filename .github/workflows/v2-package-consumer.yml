name: V2 Package Consumer

on:
  push:
    branches:
      - main
      - 'agent/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package-consumer:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore repository
        run: dotnet restore KubeJob.sln

      - name: Build repository
        run: dotnet build KubeJob.sln --configuration Release --no-restore

      - name: Pack V2 packages
        env:
          PACKAGE_VERSION: 0.0.0-smoke.${{ github.run_number }}
        run: bash eng/pack-v2.sh

      - name: Restore package consumer
        run: >-
          dotnet restore tests/KubeJob.PackageSmoke/KubeJob.PackageSmoke.csproj
          -p:KubeJobSmokeVersion=0.0.0-smoke.${{ github.run_number }}
          --source artifacts/packages
          --source https://api.nuget.org/v3/index.json

      - name: Build package consumer
        run: >-
          dotnet build tests/KubeJob.PackageSmoke/KubeJob.PackageSmoke.csproj
          --configuration Release
          --no-restore
          -p:KubeJobSmokeVersion=0.0.0-smoke.${{ github.run_number }}

      - name: Run package consumer
        run: >-
          dotnet run --project tests/KubeJob.PackageSmoke/KubeJob.PackageSmoke.csproj
          --configuration Release
          --no-build
          -p:KubeJobSmokeVersion=0.0.0-smoke.${{ github.run_number }}
